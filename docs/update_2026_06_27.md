# 图像清理工具 (Image Cleaner) 更新文档 - 2026-06-27

本更新文档详细记录了 2026-06-27 对前端性能开销、网络上传瓶颈、预览区交互机制以及 Figma PixelForge 暗色系统样式适配所做出的升级优化。

---

## 🚀 新增功能 (New Features)

### 0. Figma PixelForge 暗色系统样式适配
*   **整体视觉统一**：
    *   按 Figma `Body` 节点 (`2:3`) 的真实结构，将界面统一为深色科技工具台风格：暗蓝背景、青色发光描边、顶部状态栏、左侧参数面板、双画布预览与底部功能操作条。
    *   将主布局调整为 38px 顶部状态栏、182px 左侧侧栏、79px 底部工具栏，贴近 Figma 中 `1032 × 690` 画板的比例关系。
*   **控件风格收敛**：
    *   清理 `Toolbar.tsx` 中算法模式、导出格式、历史角标、快捷键按钮等散落的内联样式，改为 CSS class 管理，并统一为 Menlo/等宽字体、小字号、7px 控件圆角和青色激活态。
    *   为应用新增 `PixelForge` 顶部品牌栏与 GPU/内存/就绪状态展示，同时修复移动端媒体查询仍引用旧 `.toolbar` 类名的问题。
*   **品牌主题色同步**：
    *   将网页 `theme-color` 调整为 Figma 主高亮色 `#00d4ff`，使浏览器主题色与应用发光按钮一致。
*   **Figma 读取结果**：
    *   重新授权 Figma Connector 后，已通过 MCP 读取节点元数据与截图，确认主色、边框、面板尺寸、按钮圆角和状态栏文案来源于 Figma 设计稿。

### 1. 结果对比滑动条 (Before/After Split Slider UI)
*   **像素级分屏对比**：
    *   将结果预览面板 (`ResultPane.tsx`) 升级为交互式对比滑动条。如果原图和 AI 处理后的结果图同时存在，则渲染重叠视口并启用垂直滑动线与双向拖拽手柄。
    *   在顶层结果图上采用 CSS `clip-path: inset(0 0 0 X%)` 属性进行动态裁剪，无缝暴露出底层的原始图像，支持用户通过左右拖动随时比对细微的噪点消除、超分辨率重建或透明边缘切割的效果细节。
*   **HTML5 指针捕捉与边界控制**：
    *   利用 `setPointerCapture` 与 `releasePointerCapture` 对鼠标和触控指针进行锁定。即使用户在拖动过程中将光标移出浏览器窗口或预览区之外，滑动操作依然平滑可控。
*   **等比例视口锁定与暗色标签**：
    *   在 `App.tsx` 中将当前图像的 `width` 和 `height` 等比例传入，自动通过 `aspect-ratio` 限制滑动视口，保证原图与结果图完美对齐且不发生比例形变。
    *   增设左右“原图”与“效果图”的暗色半透明标签，保持与 PixelForge 青色高亮风格一致。

---

## ⚡ 性能优化 (Performance Optimizations)

### 1. IndexedDB 存储分离与高频保存零卡顿
*   **现状改善**：
    *   原先自动保存机制在每次用户绘制蒙版停笔时，都会将包含巨大原图 File 对象的整个 App 状态对象全量写入 IndexedDB 数据库，高频的磁盘 I/O 极其容易导致 UI 掉帧卡顿，且极易触发浏览器配额超限报错 (`QuotaExceededError`)。
*   **分离存储设计**：
    *   在 `AppStateStore` 对象库中新增 `"latestImageFile"` 键以独立存储原图 File 对象，而元数据则存放在 `"latestState"` 键中。
    *   在保存时增加基于 File 指纹（名称、大小、最后修改时间、引用）的缓存比对机制。仅当用户导入新图片或清空工作区时，才对 `"latestImageFile"` 进行写操作；其余高频的涂画动作，仅写入体积微小的 metadata。
    *   在加载时并发读取两部分内容并重组返回。同时保持对旧版全量存储模式的无缝向下兼容，保障老用户无痛升级。

### 2. 蒙版游程编码 (RLE) 压缩
*   **高压缩比算法**：
    *   在 `db.ts` 中针对蒙版像素数据高稀疏性的特征（由海量重复的 0 和少量的 255 构成），实现了纯手写的 `compressMaskRLE` 与 `decompressMaskRLE` 游程长度压缩算法。
    *   该算法能将数 MB 体积的 `Uint8ClampedArray` 蒙版压缩至几字节（若无涂抹）到几 KB。大幅降低了高频自动保存时的 CPU 与磁盘负载。
*   **向下兼容**：
    *   反序列化加载时如果检测到旧的未压缩 `alpha` 蒙版，则直接读取；若是新版 `rle` 数据则自动解压还原，确保升级时用户工作进度无损。

### 3. 客户端级大图预压缩与转码
*   **带宽防爆与提速**：
    *   在前端载入图片处增加 `compressAndConvertImageFile` 辅助器，用于自动拦截体积超过 10MB 的超大/无损图像文件。
    *   利用离屏 Canvas 在前端异步转码并轻度压缩为 92% 高保真质量的 WebP（含透明度）或 JPEG 格式。压缩率高达 70%-90% 以上且肉眼完全无损。
    *   此机制大幅减少了向后端接口发送修复或超分请求时的网络数据传输带宽，使超大图片上传至接口的时间缩短了数倍，并避免了由于大文件上传超时的接口报错。

---

## 🛠 修复与可靠性优化 (Fixes & Reliability)

1.  **旧版 IndexedDB 工作区迁移保护**：
    *   修复旧版 `latestState.imageFile` 加载后，首次保存可能把图片文件从元数据中清空但未写入新版 `latestImageFile` 独立槽的问题，避免刷新后丢失已恢复的旧工作区图片。
2.  **大图转码内存保护**：
    *   上传流程先检查图片尺寸，再执行客户端预压缩；`compressAndConvertImageFile` 对超高像素图像直接回退原文件，避免为超大分辨率图片创建全尺寸 Canvas 造成浏览器内存峰值。
3.  **结果对比视图安全降级**：
    *   Before/After 滑动条仅在原图与结果图尺寸一致时启用；切图等尺寸不一致的结果会自动退回单图预览，避免完整原图与局部结果硬叠造成误导。
    *   增加 `pointercancel` 与 `lostpointercapture` 收尾处理，减少触控或系统打断后拖拽状态卡住的概率。
4.  **Figma 暗色布局宽屏拉伸修复**：
    *   修复宽屏 Chrome 下应用根容器随视口无限拉伸，导致 Figma 设计稿中的 383px 双画布被放大到近千像素的问题。
    *   取消固定最大画板宽高，改为应用铺满视口、侧栏使用 `clamp()` 宽度、双画布使用 `minmax()` 自适应列，避免大屏被压缩成小块。
    *   小屏和平板断点上移至 `1024px`，布局顺序调整为顶部状态栏、编辑预览区、参数面板、底部操作条；参数面板改为紧凑网格，底部操作条改为横向滚动，避免窄屏首屏被侧栏和操作按钮挤满。
5.  **Docker 前端热更新配置**：
    *   新增 `compose.dev.yaml`，在开发模式下提供独立 `frontend-dev` 服务，用 Node + Vite dev server 替代 nginx 静态预览。
    *   通过挂载 `./frontend:/app` 和独立 `frontend-dev-node-modules` volume，让容器内 Vite 能跟随宿主机源码改动触发 HMR，同时避免 `node_modules` 跨平台污染。
    *   修复空 `node_modules` volume 会跳过依赖安装并导致 `sh: vite: not found` 的问题，启动时改为检查 `node_modules/.bin/vite` 是否存在。
    *   将前端顶层依赖从 `latest` 固定为当前主版本范围，避免 npm registry 的 dist-tag 变化导致 `npm ci` 误判 `package.json` 与 `package-lock.json` 不同步。

---

## 🧪 自动化测试验证 (Testing & Verification)

1.  **新增 Mask RLE 游程压缩测试**：
    *   在 `db.test.ts` 中补充了 4 个游程编码测试，覆盖空数组压缩、单色填充压缩、交替像素压缩以及越界溢出截断保护，全部通过。
2.  **前端测试全绿**：
    *   执行 `cd frontend && npm test`，全部 11 个前端测试文件共 57 个测试用例（包含 `db.test.ts`、`canvasExport.test.ts`、`ResultPane.test.ts` 等）100% 成功通过。
3.  **前端成功编译**：
    *   执行 `npm run build` 成功通过静态类型编译和 Vite 打包。
4.  **后端 Pytest 全绿**：
    *   执行 `.venv/bin/python -m pytest backend/tests -q`，全部 58 个测试用例（包括新增的 Celery App 与任务队列测试）通过。
